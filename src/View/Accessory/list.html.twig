{% extends 'layout.html.twig' %}

{% block content %}
    <div class="main-container">
        <h1>My accessories</h1>
        {# TODO Display all accessories here #}
        {% for accessory in accessories %}
            <article>
                <img src="{{accessory.url}}" alt="accessory image">
                <h2>{{accessory.name}}</h2>
            </article>
        {% endfor %}
        
        
    </div>
{% endblock %}
